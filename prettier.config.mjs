/** @type {import('prettier').Config} */
const config = {
  singleQuote: true,
  jsxSingleQuote: true,
  trailingComma: 'none',
  semi: true,
  plugins: ['prettier-plugin-tailwindcss']
};

export default config;
